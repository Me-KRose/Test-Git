#include "main.h"
// #include <SDL2/SDL.h>

int main(int argc, char *argv[])
{
    // if(SDL_Init(SDL_INIT_EVERYTHING) < 0)
    // {
    
    // }
    printf("Hello World!");
    int num = 63;
    printf("\nThe number is = %d",num);
    return 0;
}